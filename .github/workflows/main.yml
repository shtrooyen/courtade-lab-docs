name: Build MKdocs site

on:
  push

jobs:
  mkdocs:
    name: Build and deploy mkdocs site
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build
      uses: gcourtade/mkdocs-build-action@v0.1

